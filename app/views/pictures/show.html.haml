%p= @picture.location
= image_tag @picture.image.url(:medium)
%p
  %i
    = @picture.caption
TAGS:
%p= @picture.tag_list

COMMENTS:
<br>
- if @picture.comments.all.any?
  - @picture.comments.each do |comment|
    = comment.comments
    <br>
- else
  %p No comments yet. Why not leave the first?


= link_to "Edit #{@picture.caption}", edit_picture_path(@picture)
= link_to "Delete #{@picture.caption}", picture_path(@picture), method: :delete
= link_to "COMMENT", new_picture_comment_path(@picture)
